import Link from "next/link";

export function TopicIndex({ topics, locale, title, description, allLabel, countLabel, labels, eyebrow }: { topics: string[]; locale: string; title: string; description: string; allLabel: string; countLabel: (topic: string) => string; labels: Record<string, string>; eyebrow: string }) {
return <section className="border-y border-[#d9d2c5] bg-[#f7f3ea] text-[#191715]"><div className="mx-auto max-w-[88rem] px-[clamp(1.5rem,5vw,5rem)] py-14"><p className="font-v3-text text-xs font-bold uppercase tracking-[0.1em] text-[#e55d49]">{eyebrow}</p><div className="mt-3 flex flex-col gap-5 sm:flex-row sm:items-end sm:justify-between"><div><h2 className="font-playfair text-3xl font-semibold tracking-[-0.035em] sm:text-4xl">{title}</h2><p className="mt-2 max-w-2xl font-v3-text text-sm leading-6 text-[#6d675f]">{description}</p></div><Link href={`/${locale}/blog`} className="font-v3-text text-sm font-semibold text-[#254fa7]">{allLabel}</Link></div><nav aria-label={title} className="mt-8 flex flex-wrap gap-x-6 gap-y-3 border-t border-[#d9d2c5] pt-5">{topics.map((topic) => <Link key={topic} href={`/${locale}/blog/topics/${topic}`} className="font-v3-text text-sm text-[#5f5a53] hover:text-[#e55d49]"><span className="font-semibold">{labels[topic] ?? topic}</span><span className="ms-1 text-[#938d84]">({countLabel(topic)})</span></Link>)}</nav></div></section>;
}
