import Link from "next/link";
import type { BlogPost } from "@/lib/blog";

export function EssayArchive({ posts, locale, labels, categoryLabels, dateLabel, topicLabel, articleLabel, readTimeLabel, formatDate }: { posts: BlogPost[]; locale: string; labels: { title: string }; categoryLabels: Record<string, string>; dateLabel: string; topicLabel: string; articleLabel: string; readTimeLabel: string; formatDate: (date: string) => string }) {
  return <section id="essays" className="bg-[#fbf8f1] text-[#191715]"><div className="mx-auto max-w-[88rem] px-[clamp(1.5rem,5vw,5rem)] pb-8"><h2 className="sr-only">{labels.title}</h2><div className="hidden grid-cols-[9.5rem_11rem_minmax(0,1fr)_8rem] border-y border-[#cfc8bc] py-3 font-v3-text text-[0.68rem] font-bold uppercase tracking-[0.08em] text-[#807a72] md:grid"><span>{dateLabel}</span><span>{topicLabel}</span><span>{articleLabel}</span><span className="text-end">{readTimeLabel}</span></div><ol>{posts.map((post) => <li key={post.slug} className="border-b border-[#d8d1c5]"><Link href={`/${locale}/blog/${post.slug}`} className="group grid gap-2 py-5 transition-colors hover:bg-[#f1ece1] md:grid-cols-[9.5rem_11rem_minmax(0,1fr)_8rem] md:items-center md:px-1"><time dateTime={post.metadata.publishedAt} className="font-v3-text text-sm text-[#6d675f]">{formatDate(post.metadata.publishedAt)}</time><span className="font-v3-text text-sm font-semibold text-[#e55d49]">{categoryLabels[post.metadata.category]}</span><span><span className="block font-playfair text-[1.35rem] font-semibold leading-tight tracking-[-0.025em] sm:text-[1.55rem]">{post.metadata.title}</span><span className="mt-0.5 block font-v3-text text-sm leading-5 text-[#746e66]">{post.metadata.summary}</span></span><span className="font-v3-text text-sm text-[#6d675f] md:text-end">{post.metadata.readTime}</span></Link></li>)}</ol></div></section>;
}
